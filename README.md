# Three.js 小游戏起步项目

一个使用 TypeScript、Three.js 和 Vite 创建的最小 3D 场景。

## 启动

```bash
npm install
npm run dev
```

终端会输出本地访问地址，通常是 `http://localhost:5173`。

## 项目结构

- `src/main.ts`：创建场景、摄像机、立方体、灯光与动画循环。
- `src/style.css`：让 Three.js 画布铺满浏览器窗口。
- `index.html`：网页入口。
